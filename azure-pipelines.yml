name: 1.0$(Rev:.r)

trigger:
  branches:
    include:
    - main
    - feature/*
  paths:
    include:
    - src/**

variables:
  name: John

pool:
  name: Default

jobs:
- job: helloworld
  steps:
  - checkout: self
  - script: echo "Hello, $(name)"